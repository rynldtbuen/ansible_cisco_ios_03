
{% for c in vlans %}
    {% if c.id|int < 254 and c.id|int > 1 %}
      interface vlan{{ c.id }}
          ip address {{ svi_net }}.{{ c.id }}.{{ svi_id }} 255.255.255.0
          delay 150
          bandwidth 10000
          no shutdown
    {% endif %}
{% endfor %}
