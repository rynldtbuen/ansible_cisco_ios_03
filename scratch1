



- name: remove vlans
  ios_config:
    provider: "{{ provider }}"
    lines: no vlan {{ item.vlan_id }}
  with_items: "{{ vlans_active }}"
  when: item.vlan_id|int > 1 and item.vlan_id|int <= 1000 and item.vlan_id|int not in  vlans_var
